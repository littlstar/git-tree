{
  "name": "git-tree",
  "version": "0.0.1",
  "description": "Tree command scoped to your git project leveraging .gitignore",
  "global": "1",
  "install": "install -b git-tree.sh ${PREFIX:-/usr/local}/bin/git-tree",
  "scripts": [ "git-tree.sh" ]
}

